import * as express from 'express';
import Service from '../../../express/service.type';
declare class ContentRoute implements Service {
    constructor();
    add(): express.Router;
}
declare const _default: ContentRoute;
export default _default;
