import * as express from 'express';
import Service from '../../../express/service.type';
declare class AdminRoute implements Service {
    constructor();
    add(): express.Router;
}
declare const _default: AdminRoute;
export default _default;
